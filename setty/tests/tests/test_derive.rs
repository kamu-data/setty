/////////////////////////////////////////////////////////////////////////////////////////

#[test]
fn test_explicit_derives() {
    // Always derive Clone
    #[setty::derive(setty::Config, Clone)]
    struct A {
        x: u32,
    }

    assert_eq!(A { x: 10 }.clone(), A { x: 10 });
}

/////////////////////////////////////////////////////////////////////////////////////////
